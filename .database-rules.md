# 🛡️ ATURAN PEMISAHAN DATABASE LOCALHOST & PRODUCTION

Dokumen ini adalah aturan tetap untuk memisahkan database Localhost (Development) dan Production (Deployment).

---

## 1. Localhost (Development)
* **Tipe Database:** PostgreSQL (Lokal pgAdmin / Service)
* **Host / Port:** `127.0.0.1:5433` (atau `localhost:5433`)
* **Username / Password:** `postgres` / `almigty007`
* **Nama Database:** `postgres`
* **File Konfigurasi:** `.env` (Lokal di komputer)
* **Connection String:**
  ```env
  DATABASE_URL="postgresql://postgres:almigty007@127.0.0.1:5433/postgres?schema=public"
  DIRECT_URL="postgresql://postgres:almigty007@127.0.0.1:5433/postgres?schema=public"
  NEXTAUTH_URL="http://localhost:3006"
  ```
* **Prisma Schema Provider:**
  ```prisma
  datasource db {
    provider  = "postgresql"
    url       = env("DATABASE_URL")
    directUrl = env("DIRECT_URL")
  }
  ```

---

## 2. Production (Deploy / Netlify)
* **Tipe Database:** Supabase PostgreSQL (Cloud)
* **File Konfigurasi:** `netlify.toml` dan Netlify Dashboard Environment Variables.
* **Connection String di Netlify:**
  ```toml
  [build.environment]
  DATABASE_URL = "postgresql://postgres.xkslvfdguwvrhxetbmyw:%40Readydeath0@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
  DIRECT_URL = "postgresql://postgres.xkslvfdguwvrhxetbmyw:%40Readydeath0@aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres"
  NEXTAUTH_URL = "https://virtus-official.netlify.app"
  ```

---

## 3. Aturan Ketat (Strict Rules):
1. Provider Prisma di [`schema.prisma`](file:///c:/.mabarvip/prisma/schema.prisma) **SELALU** `provider = "postgresql"`.
2. Saat develop di laptop (`localhost:3006`), Next.js **HANYA** mengakses PostgreSQL lokal (`127.0.0.1:5433`). Apapun yang kamu tambah/edit/hapus di localhost **TIDAK AKAN PERNAH** menyentuh Supabase Production.
3. Database Supabase Production **HANYA** terpengaruh jika admin login dan mengubah data langsung di domain live production (`https://virtus-official.netlify.app/admin`).
